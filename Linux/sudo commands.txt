
reboot

uname -a, -v, -r


timedatectl


free m
df -ah
du

ip address,  ifconfig, ip addr show eth0
netstatn -tulpn

lspci

lsusb

lsblk

lsof

nutui

mtr --curses 8.8.8.8

top
ps aux | grep ngix

lsof | head
	failmode              YES   wait | continue | panic

printenv;   ( set -o posix ; set ) | less

tune2fs -l /dev/sdb1

ss -tupln

!!   #redo last

whoami
who
w
pinky
id
last

lsb_release -a
uname -a
cat /etc/os-release

hostnamectl
